"use client";

// Reuse the org implementation; it no longer depends on ScopeProvider.
import OrgDocs from "../../org/documents/page";

export default function StaffDocumentsPage() {
  return <OrgDocs />;
}
